import React from 'react'
import PropTypes from 'prop-types'

const NavBar = props => {

    const navBar = {
        width: '100%',
        height: '100px',
        background: 'brown'
    }

  return (
    <div style= { navBar }>

    </div>
  )
}
export default NavBar
